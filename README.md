# 医療費控除 明細自動作成

スマホで撮影/選択した領収書画像をOCR解析し、国税庁の「医療費集計フォーム
（iryouhi_form_v3.1.xlsx）」に自動で明細を追記していく、GitHub Pagesだけで動く
静的Webアプリです。サーバー・ビルド不要、すべてブラウザ内で完結します。

## できること

- 📷 スマホのカメラで領収書を撮影 / 画像をまとめて選択してアップロード
- 🔍 Google Cloud Vision APIで領収書から金額・日付・医療機関名・患者名を自動抽出し、
  確認・修正してから登録（OCR結果は下書きなので必ず目視確認してください）
- 👪 設定画面で家族の氏名・よく使う医療機関を事前登録しておくと、OCR結果が近い
  候補を自動選択（「医療を受けた人」はプルダウン選択制。一致しない場合は
  未選択、医療機関は一致しない場合は空欄になり手入力を促します）
- 📊 登録するたびに `iryouhi_form_v3.1.xlsx` にリアルタイムで反映（画像・ロゴ・
  シート保護・入力規則などは一切崩さず、値だけを書き込みます）
- 📅 年度ごとにタブを分けて、複数年分を別々に管理・ダウンロード可能
- ⬆ 領収書画像はGoogle Driveの指定フォルダ（年度別サブフォルダ）に自動保存可能
- 🔁 `iryouhi_form_v3.1.xlsx` がバージョンアップした場合は、設定画面からファイルを
  差し替えるだけで以降の出力に反映されます

データ（明細・設定・テンプレート）はブラウザのIndexedDBに保存されます。同じ
ブラウザ・同じ端末であれば再訪問時も続きから利用できます（別端末とは同期されません。
Drive連携をオンにしておくと領収書画像自体はDriveにバックアップされます）。

## 公開手順（GitHub Pages）

1. このフォルダの内容をGitHubリポジトリにpushする
2. リポジトリの Settings → Pages で、Source を「Deploy from a branch」、
   ブランチを `main` / フォルダを `/(root)` に設定する
3. 発行されたURL（例: `https://<user>.github.io/<repo>/`）でアプリにアクセスできる

## Google Cloudのセットアップ（OCR利用に必須）

領収書のOCR（文字認識）にはGoogle Cloud Vision APIを使用します。以下の
手順でAPIキーを発行し、アプリの ⚙設定 画面に入力してください。これを
行わないと領収書からの自動読み取りは動作しません（テンプレートへの
手入力・ダウンロード自体はAPIキーなしでも利用できます）。

1. [Google Cloud Console](https://console.cloud.google.com/) で新しいプロジェクトを作成する
2. 「APIとサービス」→「ライブラリ」から以下を有効化する
   - **Cloud Vision API**
   - （Drive連携も使う場合）**Google Drive API**、**Google Picker API**
3. [お支払い](https://console.cloud.google.com/billing) で請求先アカウントを作成し
   （クレジットカード等の登録が必要）、対象プロジェクトにリンクする
   - Cloud Vision APIは無料枠内であっても請求先アカウントの紐付けが必須です
     （Googleの不正利用防止のための仕組みで、無料枠を超えなければ実際の請求は
     発生しません）
   - 未設定のままOCRを実行すると
     `This API method requires billing to be enabled` というエラーになります
4. 「APIとサービス」→「認証情報」→「認証情報を作成」→ **APIキー**
   - 作成後、「アプリケーションの制限」で「ウェブサイト」を選び、公開した
     GitHub PagesのURL（例: `https://<user>.github.io/*`）を追加しておくことを推奨します
   - 「APIの制限」で Cloud Vision API（Drive連携も使うなら Google Picker API も）を
     許可しておくとより安全です
5. アプリの ⚙設定 画面を開き、発行したAPIキーを入力して保存

Vision APIには毎月無料枠があり、家庭で使う程度の領収書枚数であれば
通常は無料枠内に収まります（詳細は
[Cloud Vision の料金ページ](https://cloud.google.com/vision/pricing)を参照）。
領収書画像はOCRのためにGoogleのサーバーへ送信される点はご留意ください。

想定外の課金を避けたい場合は、[予算とアラート](https://console.cloud.google.com/billing/budgets)
から少額（例: 100円）の予算アラートを設定しておくと、無料枠を超える利用が
あった場合にメールで通知を受け取れます（アラートは通知のみで、自動的に
利用を止めるものではない点に注意してください）。

APIキーはブラウザのlocalStorageに保存されるのみで、リポジトリには一切含まれません。

## Google Drive連携のセットアップ（任意）

領収書画像の自動アップロードを使わない場合はこの手順は不要です。

1. 上記の手順2で **Google Drive API** と **Google Picker API** も有効化しておく
2. 左メニュー「Google Auth Platform」（旧: OAuth同意画面）を開き、未設定なら
   「開始」から設定ウィザードを進める
   - アプリ情報: アプリ名、ユーザーサポートメール（自分のGoogleアカウント）
   - 対象: 個人利用なら「外部」を選択（審査不要の「テスト」ステータスのままでOK）
   - 連絡先情報: 自分のメールアドレス → 完了
   - ウィザード完了後、左メニュー「対象」を開き、
     「テストユーザー」に自分のGoogleアカウントを追加し、
     スコープに `drive.file`（本アプリが作成/選択したファイルのみアクセス）を追加する
3. 左メニュー「Google Auth Platform」→「クライアント」→「クライアントを作成」→
   **OAuthクライアントID**
   - アプリケーションの種類: ウェブアプリケーション
   - 「承認済みのJavaScript生成元」に公開したGitHub PagesのURL
     （例: `https://<user>.github.io`）を追加する
   - 「承認済みのリダイレクトURI」は不要（空欄のままでOK）。本アプリは
     ポップアップ方式のトークンクライアントを使うため、リダイレクトURIは使用しません
   - 発行された「クライアントID」を控える
4. アプリの ⚙設定 画面を開き、クライアントIDを入力して保存
   （APIキーは上記のGoogle Cloudセットアップで入力済みのものを共用します）
5. 「保存先フォルダを選択」からGoogle Drive上の保存先フォルダを選ぶ
   - このピッカーには「新規フォルダを作成」機能がありません。新しいフォルダに
     保存したい場合は、先に [drive.google.com](https://drive.google.com) 側で
     フォルダを作成してから、ピッカーで選び直してください
6. 「登録時に領収書画像を自動でDriveへアップロードする」をオンにする

以降、明細を登録するたびに選択したフォルダの `<年度>` サブフォルダへ領収書画像が
アップロードされます。設定画面の「📂 Driveでフォルダを開く」から、保存先
フォルダをブラウザで直接開いて中身を確認できます。

アプリ内で明細を削除しても、Drive上にアップロード済みの領収書画像は削除されません
（明細データとDrive上のファイルは紐付けを持たない別々の保存先のため）。不要になった
画像はDriveのフォルダを直接開いて手動で削除してください。

## テンプレートのバージョンアップ対応

`iryouhi_form_v3.1.xlsx` が新しいバージョンに更新された場合:

1. アプリ下部の「テンプレートを差し替え」から新しいxlsxファイルを選択する
2. レイアウトが想定と異なる場合は警告が表示されます（それでも使用するか確認）
3. 以降のリアルタイム反映・ダウンロードは新しいテンプレートを使用します

テンプレートは年度ごとではなくアプリ全体で1つだけ保持する設定です。明細
データ（氏名・金額・日付など）はテンプレートとは切り離してIndexedDBに
保存されているため、年の途中でテンプレートを差し替えても、それ以前に
入力した明細が消えたり変わったりすることはありません。差し替え後に
ダウンロードすると、その時点の明細データが新しいテンプレートに書き込まれた
ファイルが生成されます（過去分・新規分が混在していても問題ありません）。
ただし、新バージョンでデータ入力範囲の行・列位置そのものが変わった場合は
下記の対応が必要です。

対応しているのは国税庁配布の「医療費集計フォーム」と同じレイアウト
（データ入力範囲: B9:J1003、列構成: 氏名/名称/区分4列/支払額/補填額/支払日）です。
レイアウトが大きく変わった新バージョンの場合は `js/xlsxPatch.js` 内の `COLS` /
`DATA_START_ROW` の調整が必要です。

## 技術構成

- 静的HTML/CSS/JSのみ（ビルド不要）。CDN経由で以下を読み込みます
  - [JSZip](https://stuk.github.io/jszip/) — xlsxの直接書き換え
  - Google Cloud Vision API — 領収書のOCR（`fetch`で直接呼び出し）
  - Google Identity Services / Google API Client — Drive連携
- xlsxは `xl/worksheets/sheet1.xml` の該当セルのみを部分的に書き換える方式のため、
  ロゴ画像・スタイル・入力規則・シート保護など元のフォーマットを完全に保持します
- 明細データ・設定・テンプレートはIndexedDB、Drive認証情報はlocalStorageに保存

## 注意事項

- OCRの認識精度は完璧ではありません。登録前に必ず内容を確認・修正してください
- 医療費控除の要件（対象となる医療費の範囲等）については国税庁の案内を
  ご確認のうえ、最終的な内容は必ずご自身で確認してください
